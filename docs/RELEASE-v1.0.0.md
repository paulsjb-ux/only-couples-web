# The Other Room v1.0.0

This release freezes the current working production application as the Version 1 baseline before the Version 2 audit/refactor begins.

The baseline should be tagged only after the version-control files are merged to `main` and the resulting production Vercel deployment is verified as healthy.
